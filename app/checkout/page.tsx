import { CheckoutClient } from "@/components/checkout-client";

export const metadata = {
  title: "Checkout | JAYAMANI EXPORT",
};

export default function CheckoutPage() {
  return (
    <main>
      <CheckoutClient />
    </main>
  );
}
